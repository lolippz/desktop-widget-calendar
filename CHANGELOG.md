# 更新记录

本项目的所有重要变更都记录在这里。
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.0.1] - 2026-09-20

### 修复

- **托盘右键菜单的中文全部显示成方框**

  现象：把窗口最小化（实际是收进系统托盘）后，在托盘图标上点鼠标右键，
  弹出的菜单里「显示日程 / 隐藏日程 / 新建日程 / 总在最前 / 透明度 / 退出」
  全部变成了空方框，完全无法辨认。

  这是 **1.0.0 打包时引入的问题**，不是界面代码的缺陷 —— 功能一直正常，
  只是文字渲染不出来。1.0.0 的源码构建（`mvn package` + `java -jar`）不受影响。

### 说明

- 功能与 1.0.0 **完全一致**，仅修复上述显示问题
- 数据格式未变，**直接覆盖安装即可，不需要重新配置**，`%APPDATA%\DesktopCalendar\` 里的日程不会丢
- 免安装包内的 exe 与 jar 版本号已同步升到 `1.0.1`

### 问题说明：为什么会变成方框

**根因是打包命令里多传了一个 `-Dfile.encoding=UTF-8`。**

这一点与直觉相反 —— 大多数"Java 中文乱码"的教程都会让你加上这个参数，
但在这个场景下它恰恰是**制造**乱码的原因。

关键在于：**AWT 的 `PopupMenu` / `MenuItem` 在 Windows 上不是 Swing 组件，
而是原生 Win32 菜单。**

- Java 把菜单项文本转成字节时，走的是 `file.encoding`
- Win32 的 ANSI 菜单 API 拿到这些字节后，按**系统 ANSI 代码页**
  （中文 Windows 上是 GBK）去解读

两边一旦不匹配，菜单项就会渲染成方框（缺字形），**而不是显示成错误的中文字符**。
正因为是"缺字形"而不是"错字"，排查时很容易被误导到字体方向上去。

实测对照（同一份菜单代码、同一字体 `Microsoft YaHei UI`，只改这一个参数）：

| JVM 参数 | `file.encoding` | 托盘菜单渲染结果 |
| --- | --- | --- |
| 不加 | `GBK` | ✅ 中文正常 |
| `-Dfile.encoding=UTF-8` | `UTF-8` | ❌ 全部方框 |

修复方式就是从打包参数里删掉它。

**为什么删掉是安全的**：本项目所有文件与网络 I/O 都**显式指定了字符集**，
不依赖运行时默认字符集：

- `HistoryService` 用 `Files.readString/writeString(..., StandardCharsets.UTF_8)`
- `Net` 的 `new String(response.body(), charset)` 显式传入 charset
- SQLite 由 sqlite-jdbc 内部按 UTF-8 处理
- `java.util.prefs.Preferences` 内部使用 UTF-8

源码里的中文字面量在**编译期**就已按 UTF-8 写进 class 文件
（由 `pom.xml` 的 `maven.compiler.encoding` 保证），与运行时默认字符集无关。

**怎么确认已经修好**：运行 1.0.1，最小化到托盘，右键托盘图标，
菜单应显示「显示日程 / 隐藏日程 / 新建日程 / 总在最前 / 透明度 / 退出」。
若仍是方框，说明 `file.encoding` 又被加回来了 ——
**注意主窗口正常并不代表菜单正常**，两者走的是完全不同的渲染路径。

> 相关细节也记在 [`BUILD.md`](BUILD.md) 的「关于两个关键参数」与
> 「3.3 验证托盘右键菜单」两节里，改动打包参数前建议先读一遍。

---

## [1.0.0] - 2026-09-20

首个正式版本。一个常驻桌面的日程挂件：农历、节假日、当天日程、股市行情、
历史上的今天，一屏扫完。

### 新增

**日程**

- 新建 / 编辑 / 删除日程：标题、地点、开始与结束时间、备注
- 重复规则：不重复、每天、每周、每月、每年，可设重复截止日期
- 提醒：可设不提醒或提前若干分钟，到点弹系统托盘气泡
- 勾选完成，已完成项在列表里弱化显示
- 冲突提示：结束时间早于开始时间、重复截止早于日程本身等会在保存前拦下

**日历**

- 农历日期、节气
- 中国法定节假日与调休，用底色区分（放假红、调休蓝），不需要额外配置
- 今日模式 / 月历模式一键切换

**桌面挂件**

- 拖动移动，八方向拖边缘缩放
- 贴边吸附：靠近屏幕边缘 24px 内自动贴齐
- 透明度六档（100 / 90 / 80 / 70 / 60 / 50），默认 92%
- 总在最前可开关
- 关闭窗口后缩到系统托盘，提醒照常工作
- 单实例保护：重复启动会激活已有窗口，不会开出第二个

**行情与资讯（可选开关）**

- 行情条：纳斯达克 100、上证指数、深证成指，涨红跌绿
- 自选股：最多 20 个，支持沪市 / 深市 / 北交所 / 美股 / 港股
- 详情弹窗：指数、自选股、财经要闻三段式
- 历史上的今天：按年份倒序，可点击跳转来源
- 刷新策略：交易时段 60 秒一次，休市 30 分钟一次

**发布形态**

- 免安装包（内置精简运行时，**不需要另外装 Java**）
- 独立可执行 fat jar（需要本机有 Java 17+）

---

[1.0.1]: https://github.com/lolippz/desktop-widget-calendar/releases/tag/v1.0.1
[1.0.0]: https://github.com/lolippz/desktop-widget-calendar/releases/tag/v1.0.0
